import { ImageUpload } from "./home";

function App() {
  return (
    <div style={{
      display: "flex",
      flexDirection: "column",
      width: "100%",
      height: "100%"
    }}>
      <ImageUpload />
    </div>
  );
}

export default App;
